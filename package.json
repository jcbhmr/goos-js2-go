{
  "type": "module",
  "devDependencies": {
    "prettier": "^3.3.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.12",
    "vitest": "^1.6.0"
  },
  "scripts": {
    "build": "vite build",
    "format": "prettier -w",
    "lint": "tsc --noEmit",
    "test": "vitest"
  }
}
